name: 'fintech_credit_analytics'
version: '1.0.0'

profile: 'fintech_credit_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

# Variabel project — override via CLI: dbt run --vars '{gcp_project_id: my-project}'
vars:
  gcp_project_id: "potent-catwalk-484317-n0"

# Materialization strategy per layer
models:
  fintech_credit_analytics:
    staging:
      +materialized: view       # Staging cukup view (hemat storage, selalu fresh)
    intermediate:
      +materialized: table
    marts:
      +materialized: table